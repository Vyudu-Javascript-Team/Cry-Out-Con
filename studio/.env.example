# Sanity Studio Configuration
# For development: Create a read-write token with `Editor` role
# For production: Create a read-only token with `Viewer` role
SANITY_STUDIO_API_TOKEN=your_api_token_here

# Your project ID (this can be public)
SANITY_STUDIO_PROJECT_ID=your_project_id_here

# Dataset to use (default: production)
SANITY_STUDIO_DATASET=production

# Allowed CORS origins (comma-separated)
SANITY_STUDIO_ALLOWED_ORIGINS=http://localhost:3000,https://cryoutcon.com,https://www.cryoutcon.com
